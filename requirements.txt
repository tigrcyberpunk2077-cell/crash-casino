aiogram>=3.7,<4
libsql-client>=0.3  # БД (локально file:, на сервере — облачный Turso, чтобы балансы не пропадали)
aiosqlite>=0.19     # нужен только для тестов
python-dotenv>=1.0
aiohttp>=3.9        # Mini App веб-сервер (обычно тянется вместе с aiogram)

# Нужно ТОЛЬКО при WALLET_PROVIDER=ton (реальный TON testnet):
# tonutils>=0.3

# --- ИИ-агент сетки каналов (модуль agent/) ---
# Бот работает на aiogram (уже в зависимостях выше). Дополнительно:
httpx>=0.27           # запросы к Gemini (LLM_PROVIDER=gemini)
anthropic>=0.40       # нужен ТОЛЬКО при LLM_PROVIDER=claude (платный API)
# Для видео-фабрики (/factory): озвучка + кадры с субтитрами. Ещё нужен системный ffmpeg.
edge-tts>=7.0         # бесплатная озвучка (TTS), без ключа
Pillow>=10.0          # впечатывание субтитров в кадры
# Для личного авто-ответчика (модуль assistant/) — работа от твоего аккаунта:
telethon>=1.36
